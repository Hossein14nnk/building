

<div class="tab-list" role="tablist">

    <% for (let [index, item] of items.entries()) { %>

        <button class="tab-item <%= index === 0 ? 'active' : '' %>" type="button" role="tab"
            data-tab="<%= item.value %>" aria-selected="<%= index === 0 ? 'true' : 'false' %>">
            <%= item.title %>
        </button>

        <% } %>

</div>

