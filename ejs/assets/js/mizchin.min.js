//  Theme Scripts
// Auto-generated file


// File: comment\script.js
class Tabs {
    constructor() {
        this.init();
    }

<<<<<<< HEAD

// File: contact-details\script.js


// File: history-timeline\script.js


// File: need-help-card\script.js


// File: news-box\script.js


// File: plan-features\script.js


// File: process-timeline\script.js


// File: project-overview\script.js


// File: services-card\script.js


// File: services-overview\script.js


// File: stats-counter\column\script.js


// File: stats-counter\row\script.js

=======
    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: contact-card\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: footer\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: items-card\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: position-card\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: quote-card\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();

// File: subscribe\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();
<<<<<<< HEAD
>>>>>>> 20d5a1da023eaad8c8e33ca564b5ca1262b800b8
=======

// File: team-feature\script.js
class Tabs {
    constructor() {
        this.init();
    }

    init() {
        document.querySelectorAll('.tab').forEach(container => {
            const tabList = container.querySelector('.tab-list');
            const labels = container.querySelectorAll('.tab-label');
            const contents = container.querySelectorAll('.tab-content');
            const position = container.getAttribute('data-tab-list-position');
            let slider = null;

            if (position === 'bottom' || position === 'top') {
                container.style.flexDirection = 'column';
            } else if (position === 'left' || position === 'right') {
                container.style.flexDirection = 'row';
                tabList.style.flexDirection = 'column';
            }

            if (container.classList.contains('tab-list-slider')) {
                slider = tabList.querySelector('.tab-slider');
                if (!slider) {
                    slider = document.createElement('div');
                    slider.classList.add('tab-slider');
                    tabList.appendChild(slider);
                }
            }

            const moveSlider = (activeLabel) => {
                if (!slider) return;

                const rect = activeLabel.getBoundingClientRect();
                const parentRect = activeLabel.parentElement.getBoundingClientRect();

                if (position === 'left' || position === 'right') {
                    const height = rect.height;
                    const top = rect.top - parentRect.top;
                    slider.style.height = `${height}px`;
                    slider.style.transform = `translateY(${top}px)`;
                    slider.style.top = '0%';
                } else {
                    const width = rect.width;
                    const left = rect.left - parentRect.left;
                    slider.style.width = `${width}px`;
                    slider.style.transform = `translateX(${left}px)`;
                    slider.style.left = '0%';
                }
            };

            const setActiveTab = (index) => {
                labels.forEach(l => l.classList.remove('active'));
                contents.forEach(c => c.classList.remove('active'));

                labels[index].classList.add('active');
                contents[index].classList.add('active');
                moveSlider(labels[index]);
            };

            labels.forEach((label, idx) => {
                label.addEventListener('click', () => setActiveTab(idx));
            });

            const activeLabel = container.querySelector('.tab-label.active');
            if (activeLabel) moveSlider(activeLabel);
        });
    }
}

new Tabs();
>>>>>>> 555892a2337d65a76876c32b6e3b6360d4179e1b
